<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MyShop | Ana Sayfa</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>

    <header class="main-header">
        <div class="container">
            <nav class="main-nav">
                <a href="/" class="logo">MyShop</a>
                <div class="nav-auth">
                    <% if (user) { %>
                        <span class="welcome-user"><%= user.username %></span>
                        <form action="/users/logout" method="POST" class="logout-form">
                            <button type="submit" class="btn btn-outline">Çıkış Yap</button>
                        </form>
                    <% } else { %>
                        <a href="/users/login" class="btn btn-outline">Giriş Yap</a>
                        <a href="/users/register" class="btn btn-primary">Kayıt Ol</a>
                    <% } %>
                </div>
            </nav>
        </div>
    </header>

    <main>
        <section class="hero">
            <div class="container">
                <h1>Mağazamıza Hoş Geldiniz</h1>
                <p>En yeni ürünleri keşfedin.</p>
            </div>
        </section>

        <section class="product-section">
            <div class="container">
                <h2>Öne Çıkan Ürünler</h2>
                <div class="product-grid">
                    <% if (products && products.length > 0) { %>
                        <% products.forEach(product => { %>
                            
                            <div class="product-card">
                                <div class="product-image">
                                    <img src="<%= (product.images && product.images.length > 0) ? '/img/' + product.images[0] : 'https://via.placeholder.com/300' %>" alt="<%= product.name %>">
                                </div>
                                <div class="product-info">
                                    <h3><%= product.name %></h3>
                                    <p><%= product.description %></p>
                                    <p class="product-price"><%= product.price %> ₺</p>
                                    <button class="btn btn-primary full-width">Sepete Ekle</button>
                                </div>
                            </div>
        
                        <% }) %>
                    <% } else { %>
                        <p>Gösterilecek ürün bulunamadı.</p>
                    <% } %>
                </div>
            </div>
        </section>
    </main>

    <footer class="main-footer">
        <div class="container">
            <p>&copy; 2025 MyShop. Tüm hakları saklıdır.</p>
        </div>
    </footer>

</body>
</html>